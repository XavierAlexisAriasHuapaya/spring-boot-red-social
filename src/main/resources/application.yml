spring:
  application:
    name: app-red-social
  datasource:
    url: jdbc:postgresql://localhost:5432/redsocial
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: create-drop

server:
  servlet:
    context-path: /api/redsocial
  port: 8080